<%= form_for(@vehicle) do |f| %>
  <% if @vehicle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vehicle.errors.count, "error") %> prohibited this vehicle from being saved:</h2>

      <ul>
      <% @vehicle.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="field">
    <%= f.label :plaque, 'Nro. Placa', {:class => "label_sistema"} %><br>
    <%= f.text_field :plaque, {:class => "input_text_sistema"} %>
  </div>
  <div class="field">
    <%= f.label :brand, 'Marca', {:class => "label_sistema"} %><br>
    <%= f.text_field :brand, {:class => "input_text_sistema"} %>
  </div>
  <div class="field">
    <%= f.label :color, 'Color', {:class => "label_sistema"} %><br>
    <%= f.text_field :color, {:class => "input_text_sistema"} %>
  </div>

  <div class="field">
    <%= f.label :category, 'Categoría', {:class => "label_sistema"} %><br>
    <%= f.text_field :category, {:class => "input_text_sistema"} %>
  </div>
  <div class="field">
    <%= f.label :driver_id, 'Chofer', {:class => "label_sistema"} %><br>
    <%= f.collection_select(:driver_id, Driver.all, :id,  :name) %>
  </div>
  <div class="field">
    <%= f.label :status, 'Estado', {:class => "label_sistema"} %><br>
    <%= f.check_box :status, {:class => "input_text_sistema"} %>
  </div>

  <div class="actions">
    <%= f.submit '  Grabar  ', {:class => "input_bottom_sistema"} %>
  </div>
<% end %>
